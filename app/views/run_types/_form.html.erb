<%= form_for(@run_type) do |f| %>
  <% if @run_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@run_type.errors.count, "error") %> prohibited this run_type from being saved:</h2>

      <ul>
      <% @run_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :label %><br />
    <%= f.text_field :label %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
