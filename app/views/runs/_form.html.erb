<%= form_for(@run) do |f| %>
  <% if @run.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@run.errors.count, "error") %> prohibited this run from being saved:</h2>

      <ul>
      <% @run.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :hill %><br />
    <%= f.text_field :hill %>
  </div>
  <div class="field">
    <%= f.label :run_type %><br />
	<%= select_tag 'run_type', options_from_collection_for_select(@run_types,:id,:label) %>
  </div>
  <div class="field">
    <%= f.label :total %><br />
    <%= f.number_field :total %>
  </div>
  <div class="field">
    <%= f.label :open %><br />
    <%= f.number_field :open %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
